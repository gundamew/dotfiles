# Wow
[user]
    name = Bing-Sheng Chen
    email = gundamew@gmail.com

# Such alias
[alias]
    st = status
    ci = commit
    co = checkout
    lg = log --pretty=lg
    di = diff
    br = branch
	alias = config --get-regexp ^alias\\.
[pretty]
	lg = %Cred%h%Creset -%C(yellow)%d%Creset %s %Cgreen(%ci) %C(blue bold)<%an>%Creset

# Very core
[core]
    autocrlf = input
    excludesFile = ~/.gitignore_global
    pager = diff-highlight | less
    editor = code --wait
[branch]
    autoSetupRebase = always
[branch "master"]
    mergeOptions = --no-ff
[checkout]
    defaultRemote = origin
[commit]
    template = ~/.gitmessage
[pull]
    rebase = true
[rebase]
    autoStash = true
[diff]
    algorithm = patience
[pager]
    log = diff-highlight | less
    show = diff-highlight | less
    diff = diff-highlight | less

# So much color
[color]
    ui = auto
    branch = auto
    diff = auto
    status = auto
[color "branch"]
    current = yellow reverse
    local = yellow
    remote = green
[color "diff"]
    meta = yellow bold
    commit = green bold
    frag = magenta bold
    old = red bold
    new = green bold
    whitespace = red reverse
[color "diff-highlight"]
    oldNormal = red bold
    oldHighlight = red bold 52
    newNormal = green bold
    newHighlight = green bold 22
[color "status"]
    added = yellow
    changed = green
    untracked = cyan
